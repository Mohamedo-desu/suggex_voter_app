import React from "react";
import { StyleSheet, Text, View } from "react-native";

const SuggestionDetails = () => {
  return (
    <View>
      <Text>suggestionDetails</Text>
    </View>
  );
};

export default SuggestionDetails;

const styles = StyleSheet.create({});
